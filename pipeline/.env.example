# Direct (non-pooled) Neon connection string. Used by ingest.py, a
# one-off batch script with no concurrent connections — the pooled
# connection matters for apps/api later, not here.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"